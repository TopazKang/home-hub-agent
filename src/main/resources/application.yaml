spring:
  application:
    name: home-hub-agent

server:
  port: ${SERVER_PORT:8081}

node:
  port: ${NODE_PORT:8021}
  container-name: ${NODE_CONTAINER_NAME:palworld}

docker:
  host: ${DOCKER_HOST:unix:///Users/topazkang/Library/Containers/com.docker.docker/Data/docker-cli.sock}